    GOAL 1: Database Initialization (data.csv)
        • Step 1.1: Open data.csv using std::ifstream. If the file cannot be opened, throw an exception or terminate with an error message on std::cerr.

        • Step 1.2: Read the first line using std::getline and verify it matches the header format (date,exchange_rate).

        • Step 1.3: Loop through the remaining lines. Split each string into two components using the comma , as a delimiter.

        • Step 1.4: Convert the rate component into a floating-point number. Validate that the value is parsed cleanly.

        • Step 1.5: Insert the entry into your std::map<std::string, float> database.



    GOAL 2: Input File Processing & String Parsing
        • Step 2.1: Verify argc == 2. Open the file specified by argv[1]. If it fails, output Error: could not open file. to standard output as shown in the subject example.

        • Step 2.2: Execute a line-by-line loop using std::getline. If a line is empty, skip it or output a bad input error depending on style choice (the correction sheet states: "The program must not stop its execution before having performed the operations on the whole file").

        • Step 2.3: Locate the pipe delimiter |. If it is absent, immediately print an error message (Error: bad input => [line_content]) and proceed to the next iteration.



    GOAL 3: Deep Validation Phase
        • Step 3.1: Date Formatting: Ensure the date string strictly conforms to YYYY-MM-DD (exactly 10 characters, with hyphens at index 4 and 7). Extract the Year, Month, and Day as integers.

        • Step 3.2: Chronological Limits: * Month must be between $1$ and $12$.
            • Day must be valid for that specific month: Months 1, 3, 5, 7, 8, 10, 12 have up to 31 days. Months 4, 6, 9, 11 have up to 30 days.
            • For February (Month 2), implement a leap-year validation check: Day can be up to 29 if $\text{Year} \pmod 4 == 0$ (and if $\text{Year} \pmod{100} == 0$, it must also be divisible by $400$); otherwise, Day can be at most 28.

        • Step 3.3: Value Constraints: Convert the value string to a float.
            • If the value is negative, print Error: not a positive number.
            • If the value exceeds $1000$, print Error: too large a number.
            • If the string contains invalid trailing alpha characters or fails to parse, treat it as a bad input error.



    GOAL 4: Historical Database Querying
        • Step 4.1: Call database.upper_bound(input_date). This returns an iterator to the first element whose key is strictly greater than the requested date.

        • Step 4.2: Handle boundary scenarios:If the returned iterator equals database.begin(), it means the input date is older than any data point available in your database. Print an out-of-bounds historical error.Otherwise, decrement the iterator by one (--iterator). This moves it to the largest historical date that is less than or equal to your input date, which perfectly matches the assignment requirement.

        • Step 4.3: Multiply the user's input value by the exchange rate stored at that iterator location. Output the result in the format: YYYY-MM-DD => input_value = calculated_result.

